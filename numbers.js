// find a sum of input numbers

let firstNumber = +prompt('ведите первое число', 0);
let secondNumber = +prompt('введите второе число', 0);

alert(firstNumber + secondNumber);

// a sum with accuracy
alert((0.3 * 10 + 0.2 * 10) / 10);
